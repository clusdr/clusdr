# `clusdr version`

Prints build version, commit, and time. Works without a daemon.

## Synopsis

```bash
clusdr version
```

A release binary prints the tag. A source build prints `dev` unless ldflags set `Version`.

## See also

- [CLI index](README.md)
- [Compatibility](../compatibility.md)
